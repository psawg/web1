window.addEventListener('DOMContentLoaded', function() {
    // Lấy phần tử body của trang
    var body = document.querySelector('body');
    
    // Thêm lớp CSS "scrollbar" vào phần tử body
    body.classList.add('scrollbar');
  });